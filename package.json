{
  "name": "xray-scanner",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A CreepJS-style, catalog-driven fingerprint / bot-detection test page. Actively probes every surface in the script2builtins-knowledge catalog so you can diff a real browser against a bot and benchmark anti-detect products.",
  "license": "MIT",
  "author": "Tyler Richards <tyler@tyler-richards.com>",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview",
    "coverage": "tsx scripts/coverage-check.ts",
    "test": "tsx tests/scoring.test.ts",
    "prebuild": "npm run coverage"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "script2builtins": "^0.1.1"
  }
}
